@page
@model Prn221_8_HoaLan.Pages.Auctions.RegisterAuctionProductModel
@{
    Layout = "_Layout";
}
@using DataAccessLayer.Model
@{
    var fullName = "";
    var session = HttpContext.Session;
    User user = Prn221_8_HoaLan.SessionExtensions.Get<User>(session, "User");
    if (user != null)
    {
        fullName = user.FirstName + " " + user.LastName;
        ViewBag.Role = user.Role;
    }
}

<div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="border p-4 col-md-8 mx-auto">
        <h1 class="text-center mb-4">Product Register</h1>
        <form method="post" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="auctionName" class="form-label">Name of the Auction</label>
                <input type="text" class="form-control" id="auctionName" name="auctionName">
            </div>
            <div class="mb-3">
                <label for="productName" class="form-label">Name of Product</label>
                <input type="text" class="form-control" id="productName" name="productName">
            </div>
            <div class="mb-3">
                <label for="productDesc" class="form-label">Description of Product</label>
                <textarea class="form-control" id="productDesc" name="productDesc" rows="4"></textarea>
            </div>
            <div class="mb-3">
                <label for="imageInput" class="form-label">Select Image</label>
                <input type="file" class="form-control" id="imageInput" name="imageFile" />
            </div>
            <div class="mb-3 text-center">
                <button type="submit" class="btn btn-primary">Register Product</button>
            </div>
        </form>
    </div>
</div>
