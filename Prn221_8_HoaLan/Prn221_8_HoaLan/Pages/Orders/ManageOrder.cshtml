@page
@using DataAccessLayer.Model
@model Prn221_8_HoaLan.Pages.Orders.ManageOrderModel
@{
    Layout = "_Layout";
}
@{
    var fullName = "";
    var session = HttpContext.Session;
    User user = Prn221_8_HoaLan.SessionExtensions.Get<User>(session, "User");
    if (user != null)
    {
        fullName = user.FirstName + " " + user.LastName;
        ViewBag.Role = user.Role;
    }
}

<div class="d-flex flex-column mx-4 mt-3" style="min-height: 100vh">
    @foreach (Order o in Model.orderList)
    {
        <div class="h-50 py-2 px-3 mb-3" style="border-color: black; border: 1px solid; border-radius: 13px">
            <a asp-page="./OrderDetail" asp-route-id="@o.OrderId">
                <div class="d-flex flex-column">
                    <div>
                        order:
                        @o.OrderDate.ToString().Split("12:00:00")[0]
                    </div>
                    <div>
                        total price:
                        @o.TotalPrice vnd
                    </div>
                </div>
            </a>


        </div>
    }

</div>